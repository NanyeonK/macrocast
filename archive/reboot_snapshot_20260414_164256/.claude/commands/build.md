Implement or extend a module in the macrocast package.

Instructions:
1. Read the design document at docs/DESIGN.md for the full architecture
2. Check existing code in the target layer to understand current state
3. Write the implementation with full type hints and NumPy-style docstrings
4. Write corresponding tests in tests/ mirroring the source structure
5. Run `uv run pytest tests/ -v --tb=short` to verify
6. Run `uv run ruff check macrocast/` to lint

Target: $ARGUMENTS

Always explain what you're building and why before writing code. After implementation, summarize what was done and what remains.
